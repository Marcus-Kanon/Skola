using NUnit.Framework;
using Övning_3;

namespace Övning_3_Tests
{
    public class Tests
    {
        [SetUp]
        public void Setup()
        {
        }

        [Test]
        public void PushTest()
        {/*
            MyList<string> Tester = new();

            Tester.Push("d");
            Tester.Push("c");
            Tester.Push("b");
            Tester.Push("a");

            string expected = "b";
            string actual = Tester.Get(1);

            Assert.AreEqual(expected, actual);*/
        }
    }
}